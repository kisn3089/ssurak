import styles from "@/components/landing/landing.module.css";

export default function Eyebrow() {
  return (
    <p
      className={`text-sm font-semibold mb-5 text-teal tracking-wider ${styles.mono}`}
    >
      TABLE ORDER
    </p>
  );
}
